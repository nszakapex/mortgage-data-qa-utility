# Streamlit Community Cloud installs from this file (uv, with pip fallback).
# Prefer plain wheels over an editable local install so Cloud does not need a
# package build. The app bootstraps src/ onto sys.path for local imports.
pandas>=2.2,<3.0
openpyxl>=3.1,<4.0
streamlit>=1.36,<2.0
